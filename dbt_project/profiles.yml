econ_database:
  target: "{{ env_var('DBT_TARGET', 'dev') }}"
  outputs:
    dev:
      type: bigquery
      method: oauth
      project: "{{ env_var('BIGQUERY_PROJECT') }}"
      dataset: economics_staging
      threads: 4
      location: "{{ env_var('BIGQUERY_LOCATION', 'US') }}"
      timeout_seconds: 300

    prod:
      # On GCE, ADC automatically uses the attached VM service account —
      # no keyfile needed. Ensure the service account has bigquery.dataEditor
      # and bigquery.jobUser on the project (provisioned in Terraform Phase 0).
      type: bigquery
      method: oauth
      project: "{{ env_var('BIGQUERY_PROJECT') }}"
      dataset: economics_staging
      threads: 8
      location: "{{ env_var('BIGQUERY_LOCATION', 'US') }}"
      timeout_seconds: 300
