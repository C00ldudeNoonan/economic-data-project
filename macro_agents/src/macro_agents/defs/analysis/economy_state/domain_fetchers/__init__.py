"""Domain-specific data fetchers for the economy-state sub-agents.

Each module fetches BigQuery data for one sub-agent domain (labor, financial,
commodities, sectors, market structure). Import the specific fetcher you need
from its domain module — this package intentionally has no barrel re-exports.
"""
